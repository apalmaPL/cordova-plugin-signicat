repositories {
    flatDir {
        dirs "${projectDir}/libs"
    }
}

dependencies {
   implementation(name:'android-sdk-1.1.7', ext:'aar')
}

android {
    defaultConfig {
        manifestPlaceholders = [
            appAuthRedirectScheme: "https",
            appAuthRedirectHost: "testpilot.hollandzorg.com"
        ]
    }
}

